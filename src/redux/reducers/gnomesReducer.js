import actionTypes from '../actions/actionTypes';

export default function gnomesReducer(action) {
  switch (action.type) {
    case actionTypes.LOAD_GNOMES:

      break;

    default:
      break;
  }
}
